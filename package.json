{
  "dependencies": {
    "typia": "^6.8.0"
  },
  "devDependencies": {
    "ts-patch": "^3.2.1",
    "typescript": "^5.5.2"
  },
  "scripts": {
    "build": "typia generate --input src --output build",
    "prepare": "ts-patch install && typia patch"
  }
}
